import { Resend } from "resend";
export async function sendTransactionalEmail(input:{to:string;subject:string;html:string}){const key=process.env.RESEND_API_KEY;if(!key)return{ok:false as const,error:"RESEND_API_KEY is not configured."};try{const resend=new Resend(key);const result=await resend.emails.send({from:process.env.EMAIL_FROM||"TRConcept <hello@trconcept.co>",replyTo:process.env.EMAIL_REPLY_TO||"hello@trconcept.co",to:input.to,subject:input.subject,html:input.html});if(result.error)return{ok:false as const,error:result.error.message};return{ok:true as const,id:result.data?.id||null}}catch(error){return{ok:false as const,error:error instanceof Error?error.message:"Unknown email error"}}}