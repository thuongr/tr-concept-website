[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "22"

# Modern Next.js (13.5+) is deployed by Netlify's OpenNext adapter automatically.
# Do not pin the legacy @netlify/plugin-nextjs plugin here.
