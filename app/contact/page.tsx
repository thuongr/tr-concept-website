"use client";
import { useSearchParams } from "next/navigation";
import { FormEvent, Suspense, useState } from "react";
function ContactForm(){const params=useSearchParams();const type=params.get("type")||"general";const[message,setMessage]=useState("");async function submit(event:FormEvent<HTMLFormElement>){event.preventDefault();const form=new FormData(event.currentTarget);const response=await fetch("/api/contact",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({type,name:String(form.get("name")||""),email:String(form.get("email")||""),business:String(form.get("business")||""),message:String(form.get("message")||"")})});setMessage(response.ok?"Got it — your message has been received. Check your email for confirmation.":"Something went wrong. Please try again.");if(response.ok)event.currentTarget.reset()}return <section className="page-hero"><div className="shell narrow"><p className="eyebrow">Contact</p><h1>Tell me what you’re working on.</h1><p className="page-lead">Bring the real situation. We can work out the sensible next step from there.</p><form className="form-card contact-form" onSubmit={submit}><div className="form-grid"><label>Name<input name="name" required/></label><label>Email<input name="email" type="email" required/></label><label className="form-full">Business / role <span>(optional)</span><input name="business"/></label><label className="form-full">What are you trying to do?<textarea name="message" rows={6} required/></label></div><button className="button button-yellow" type="submit">Send enquiry →</button>{message&&<p className="form-message">{message}</p>}</form></div></section>}
export default function Page(){return <Suspense><ContactForm/></Suspense>}